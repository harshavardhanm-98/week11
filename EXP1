#include <stdio.h>
#include <string.h>
struct Book {
    char title[50];
    char author[50];
    float price;
};
int main() {
    struct Book b1;
    strcpy(b1.title, "C Programming");
    strcpy(b1.author, "Dennis Ritchie");
    b1.price = 450.50;
    printf("Book Details:\n");
    printf("Title: %s\nAuthor: %s\nPrice: %.2f\n", b1.title, b1.author, b1.price);
    printf("\nREG NO:25331A05I5");
    return 0;
}
